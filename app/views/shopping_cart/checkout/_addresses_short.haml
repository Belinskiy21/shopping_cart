%p.general-address
  = address.first_name + ' ' + address.last_name
  - if step != :complete
    = link_to  t('edit'), wizard_path(:addresses), class: 'general-edit clickable-step'
  %br
  = address.address
  %br
  = address.city + ' ' + address.zip.to_s
  %br
  = address.country
  %br
  = 'Phone ' + address.phone
